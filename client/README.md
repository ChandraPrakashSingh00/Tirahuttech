# Tirahut — Client

React + Vite frontend. See the [root README](../README.md) for full setup, architecture, and Docker instructions.

Quick start:
```bash
cp .env.example .env   # set VITE_API_URL
npm install
npm run dev
```
